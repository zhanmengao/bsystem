0.9.1
1、修复RocketMQ客户端中收到数据偶现乱码问题。
2、修复在双MQ情况下，导致第二个MQ无效的问题。
3、日志增加来源【Service】【VEIDP】
4、新增20号指令，调用脚本终止程序（提前部署）

0.9.2
1、服务程序新增自动更新功能
自动更新功能：增加更新类型：强制更新，普通更新
当最新版本号为空，或版本号字符串完全一致时，不调用更新函数。
强制更新将强制覆盖当前版本信息进行更新。
普通更新会对比版本号，若新版本领先于当前版本，才会进行更新。

0.9.3
1、修改采集工具，使用更加安全的版本，避免Windows防护机制拦截。
2、新增19号指令，设置orgcode,下次启动后生效。
3、修复因COM被CoUninitialize()导致WMI调用失败的问题。
4、在发布时能够静默安装并调用smve加服务。

0.9.4
1、修复因COM初始化在其他线程，导致WMI调用失败的问题。
2、修复因“在整理或打乱任何接口之前，必须初始化安全机制。一旦初始化，不能再作更改。”错误发生时，会马上析构COM问题。说明CoInitializeSecurity 已经被调用过，这不算是错误。
3、修改自动更新程序无法更新二进制文件的问题。
4、修复在网线断开的情况下，注册消费者会导致程序退出的问题。
5、修复在上一次缓存了新的版本信息的情况下，若服务启动时网络不通，会误认为更新成功并写入版本号。
6、修复当上一次发送失败产生的消息缓存过大时，veidp.exe缓冲区溢出的问题。

0.9.5
1、解决在https情况，热更新下载文件失败的问题。
2、现在整包安装，可以支持覆盖安装了。
3、解决在先联网，后断网情况下，rocketMQ客户端段错误导致客户端崩溃的问题。

0.9.6
1、适配小米电视机作为显示器