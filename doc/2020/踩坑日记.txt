1.Windows注册时钟消息SetTimer函数须在主线程调用，如果在其他线程调用会导致收到消息延迟。
2.WMI调用保证：
①整个进程只有一条连接，且不被多个线程同时调用。
②每次调用结束后需要断开连接。
否则会导致“应用程序调用一个已为另一线程整理的接口”。

3.在进程中加的锁在退出时必须全部解锁，否则会导致“Debug Error”。std::lock(lockA,lockB)不会自动解锁
